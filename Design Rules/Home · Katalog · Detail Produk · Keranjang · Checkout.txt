Before designing any page in this batch, internalize this complete JavaShop sitemap. Every element you place must resolve to one of these destinations or actions. No orphan elements allowed.

═══════════════════════════════════════
JAVASHOP — SITEMAP & URL STRUCTURE
═══════════════════════════════════════
PUBLIC PAGES (accessible without login):
  /                          → Beranda (Home)
  /produk                    → Katalog Produk
  /produk?kategori=[slug]    → Katalog filtered by category
  /produk?q=[query]          → Katalog search results
  /produk/[slug]             → Detail Produk
  /login                     → Halaman Login
  /daftar                    → Halaman Register
  /auth/lupa-sandi           → Lupa Kata Sandi
  /auth/reset-sandi          → Reset Kata Sandi

CUSTOMER PRIVATE PAGES (requires login, redirect to /login?redirect=[page] if not):
  /keranjang                 → Keranjang Belanja
  /checkout                  → Halaman Checkout
  /checkout/pembayaran       → Halaman Pembayaran (Midtrans / upload bukti)
  /pesanan/berhasil          → Order Success / Konfirmasi
  /akun                      → Profil Saya
  /akun/pesanan              → Riwayat Pesanan
  /akun/pesanan/[order-id]   → Detail Pesanan & Tracking
  /akun/wishlist             → Wishlist
  /akun/ulasan               → Ulasan Saya
  /akun/notifikasi           → Notifikasi
  /akun/alamat               → Kelola Alamat
  /akun/ubah-sandi           → Ubah Kata Sandi

ADMIN PAGES (requires admin role):
  /admin                     → Admin Dashboard
  /admin/produk              → Daftar Produk
  /admin/produk/tambah       → Form Tambah Produk
  /admin/produk/[id]/edit    → Form Edit Produk
  /admin/pesanan             → Daftar Pesanan
  /admin/pesanan/[id]        → Detail Pesanan Admin
  /admin/pembayaran          → Konfirmasi Pembayaran
  /admin/voucher             → Daftar Voucher
  /admin/pengguna            → Daftar Pengguna
  /admin/ulasan              → Moderasi Ulasan
  /admin/laporan             → Laporan Penjualan

EXTERNAL SERVICES:
  Midtrans payment page      → https://app.midtrans.com/...
  Biteship tracking          → https://biteship.com/tracking/[resi]


═══════════════════════════════════════
BATCH PROMPT — 5 HALAMAN CUSTOMER UTAMA
═══════════════════════════════════════

Design 5 pages for JavaShop, a premium artisan coffee e-commerce website. Follow the design system rules provided. Desktop width: 1440px. Use Cormorant Garamond for headings, DM Sans for body. Primary accent: #B8924A (gold). Background: #FAF7F2 (warm off-white). All pages share the same navigation bar.

SHARED NAVIGATION BAR (appear on all 5 pages):
Height 72px, bg #FAF7F2, bottom border 1px #E0D0BB, sticky on scroll.
Left: JavaShop logo (coffee bean mark + wordmark "JavaShop" in Cormorant Garamond 600 22px, #1C0A00) → NAVIGATE /
Center: Nav links — DM Sans 400 14px #1C0A00, active link in #B8924A with 2px bottom underline:
  "Beranda" → NAVIGATE /
  "Produk" → NAVIGATE /produk
  "Tentang Kami" → STATE smooth scroll to #tentang-kami section
  "Kontak" → STATE smooth scroll to #kontak section
Right icons:
  Search icon (Phosphor MagnifyingGlass, 20px) → STATE search bar expands inline in navbar, input focused
  Wishlist heart icon with count badge → IF logged in: NAVIGATE /akun/wishlist / IF not logged in: NAVIGATE /login?redirect=/akun/wishlist
  Cart icon (Phosphor ShoppingCart, 20px) with gold number badge → NAVIGATE /keranjang
  User avatar circle (if logged in) → STATE dropdown opens with: "Profil Saya" → NAVIGATE /akun | "Riwayat Pesanan" → NAVIGATE /akun/pesanan | "Keluar" → ACTION clear session → NAVIGATE /
  "Masuk / Daftar" link (if not logged in) → NAVIGATE /login
Mobile: Hamburger menu replaces nav links.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PAGE 1 — BERANDA (HOME)
Post-login home page for returning logged-in users.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

INTENT: Drive logged-in users deeper into the product catalogue and towards adding items to their cart. Every section pushes toward product discovery and purchase.

── HERO SECTION (full-bleed, height 680px) ──
Full-width warm atmospheric photo (coffee beans on a wooden table, warm low-light photography). Dark overlay rgba(28,10,0,0.45). Content centered-left at 80px from left:
  — Small label above headline: "Specialty Coffee · Langsung dari Petani" in DM Sans 400 12px letter-spacing 0.12em, color #B8924A
  — Main headline: "Rasakan Kopi\nTerbaik Nusantara" in Cormorant Garamond 600 64px, color #F0E8D8, line-height 1.1, max-width 520px
  — Subtext below: "Kami menghadirkan biji kopi pilihan dari perkebunan terbaik Aceh, Toraja, Flores, dan Papua — langsung ke tangan Anda." in DM Sans 400 16px, color #E0D0BB, max-width 480px, line-height 1.7, margin-top 20px
  — Two buttons side by side (margin-top 32px):
      [Element: Tombol 'Belanja Sekarang']
      Style: Primary gold button, bg #B8924A, text #FAF7F2, DM Sans 500 14px, radius 6px, padding 14px 28px
      States: Default → Hover (bg #9A7735, translateY -1px) → Active (scale 0.97)
      Behavior: → NAVIGATE /produk
      Annotation: 'NAVIGATE /produk'

      [Element: Tombol 'Lihat Koleksi']
      Style: Ghost button, transparent bg, border 1.5px #F0E8D8, text #F0E8D8, radius 6px
      States: Default → Hover (bg rgba(240,232,216,0.15), translateY -1px)
      Behavior: → STATE smooth scroll to #produk-unggulan section
      Annotation: 'STATE → scroll to #produk-unggulan'

  — Bottom of hero: 3 stats in a horizontal row with thin gold vertical dividers: "500+ Produk" | "10.000+ Pelanggan" | "15 Daerah Asal" — each in Cormorant Garamond 600 28px gold above DM Sans 12px #E0D0BB label

── CATEGORY STRIP (height 120px, bg #F0E8D8) ──
Horizontally scrollable category pills, centered:
5 category cards (140×80px each, rounded 8px, bg #FAF7F2, border 1px #E0D0BB):
Each has a small coffee-related Phosphor icon above the text.
  "Arabika" → NAVIGATE /produk?kategori=arabika
  "Robusta" → NAVIGATE /produk?kategori=robusta
  "Blend" → NAVIGATE /produk?kategori=blend
  "Single Origin" → NAVIGATE /produk?kategori=single-origin
  "Decaf" → NAVIGATE /produk?kategori=decaf
States: Default (bg #FAF7F2) → Hover/Active (bg #B8924A, text #FAF7F2, icon #FAF7F2)

── FEATURED PRODUCTS SECTION (id="produk-unggulan") ──
Section heading centered: "Produk Unggulan" in Cormorant Garamond 600 40px. Subheading: "Pilihan terbaik kami minggu ini" in DM Sans 400 16px #6B4C35.
Below: 4-column product card grid. Each card: 280×360px, rounded 12px, bg #F0E8D8, shadow 0 2px 12px rgba(28,10,0,0.06).
  — Image area: 280×224px portrait, rounded-top 12px, warm product photo
  — "Terlaris" gold badge top-left of image area
  — Product name: Cormorant Garamond 500 20px
  — Origin: DM Sans 400 12px #6B4C35 (e.g. "Aceh Gayo · Arabika")
  — Price: DM Sans 500 16px #B8924A
  — Star rating: 5 stars (4 filled gold, 1 outline), review count "(128)" DM Sans 12px

  [Element: Product Card (click anywhere on card)]
  Behavior: → NAVIGATE /produk/[product-slug]
  States: Default → Hover (card lifts translateY -4px, shadow intensifies 0 8px 32px rgba(28,10,0,0.12), image zooms scale 1.03, "Tambah ke Keranjang" overlay appears)

  [Element: 'Tambah ke Keranjang' overlay button on hover]
  Style: Dark overlay on image bottom, DM Sans 500 14px, padding 12px
  Behavior: → IF logged in: ACTION POST /cart/add → cart counter badge +1, success toast "Produk ditambahkan!" 3 detik / IF not logged in: NAVIGATE /login?redirect=/produk/[slug]
  Annotation: 'IF logged in: POST /cart/add + toast / ELSE: NAVIGATE /login'

  [Element: Heart/Wishlist icon top-right of image]
  Style: Phosphor Heart 20px, outline #FAF7F2 on image
  Behavior: → IF logged in: ACTION TOGGLE wishlist → icon fills solid gold (added) or unfills outline (removed) + subtle scale bounce / IF not logged in: NAVIGATE /login?redirect=/produk/[slug]
  States: Inactive (outline) → Active (filled gold) → Hover (scale 1.1)
  Annotation: 'IF logged in: TOGGLE wishlist / ELSE: NAVIGATE /login'

── ABOUT STRIP (full-bleed, bg #1C0A00, height 400px, id="tentang-kami") ──
Two columns (50/50):
Left: warm coffee roasting photo, rounded 16px
Right: text block centered vertically — "Mengapa JavaShop?" in Cormorant Garamond 600 40px #F0E8D8. 3 feature rows below (icon + title + description):
  • Package icon → "Kemasan Kedap Udara" → "Menjaga kesegaran biji kopi hingga 12 bulan."
  • Truck icon → "Pengiriman Cepat" → "Dikirim via Biteship ke seluruh Indonesia."
  • Star icon → "Kualitas Terjamin" → "Hanya biji kopi grade A dari petani terseleksi."
Each row: icon 24px gold · title DM Sans 500 16px cream · desc DM Sans 400 14px #A8886A. 16px gap between rows.

── TESTIMONIAL SECTION ──
Section heading: "Kata Mereka" in Cormorant Garamond 600 40px, centered.
3 testimonial cards (360px wide, rounded 12px, bg #F0E8D8). Each: large quotation mark " in Cormorant Garamond 600 64px #B8924A, testimonial text DM Sans 400 16px italic, reviewer name DM Sans 500 14px, origin "Jakarta · Pelanggan Setia" DM Sans 12px #6B4C35, 5 gold stars.

── NEWSLETTER SECTION (full-bleed, bg #F0E8D8, py 64px) ──
Centered: "Dapatkan Promo Terbaik" H2 + subtext + email input (wide, 400px) + "Langganan" primary button side by side.

  [Element: Email input field]
  Style: 400px wide, 48px height, border 1.5px #E0D0BB, radius 8px, placeholder "Masukkan email Anda"
  States: Empty → Focus (border #B8924A, glow) → Filled → Error (border #C0392B, "Format email tidak valid" below)

  [Element: Tombol 'Langganan']
  Style: Primary gold button, bg #B8924A, DM Sans 500 14px
  Behavior: → ACTION validate email → IF valid: submit email → inline success message "Berhasil! Cek email Anda." replaces form / IF invalid: show error "Format email tidak valid"
  States: Default → Hover → Loading (spinner + "Memproses...") → Disabled (after success)
  Annotation: 'ACTION → submit newsletter email'

── FOOTER (bg #140C05, id="kontak") ──
4 columns:
  Brand: logo + tagline "Setiap cangkir, sebuah cerita." + social icons:
    Instagram icon → EXTERNAL https://instagram.com/javashop
    TikTok icon → EXTERNAL https://tiktok.com/@javashop
    WhatsApp icon → EXTERNAL https://wa.me/6281200000000
  Navigasi:
    "Beranda" → NAVIGATE /
    "Produk" → NAVIGATE /produk
    "Tentang Kami" → NAVIGATE /#tentang-kami
    "Blog" → NAVIGATE /blog
  Bantuan:
    "FAQ" → NAVIGATE /faq
    "Cara Pemesanan" → NAVIGATE /cara-pemesanan
    "Kebijakan Pengembalian" → NAVIGATE /kebijakan-pengembalian
    "Hubungi Kami" → NAVIGATE /kontak
  Kontak:
    Email: hello@javashop.id
    WhatsApp: 0812-xxxx-xxxx
    Jam operasional: Sen-Sab 08.00–17.00 WIB
Bottom bar: © 2025 JavaShop · "Pembayaran aman via" + Midtrans logo + "Pengiriman oleh" + Biteship logo.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PAGE 2 — KATALOG PRODUK (/produk)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

INTENT: Help users efficiently discover and filter through the product catalogue. Every filter, sort, and card interaction must lead to a meaningful result.

Page header below navbar: Full-bleed 240px warm photo strip with dark overlay. Centered text: "Koleksi Kopi Kami" H1 #F0E8D8 + breadcrumb "Beranda / Produk" DM Sans 12px #B8924A.
  Breadcrumb "Beranda" → NAVIGATE /

MAIN CONTENT (sidebar + grid layout):

Left sidebar (280px fixed, top-aligned):
  [Element: Search bar]
  Style: Full-width input, Phosphor MagnifyingGlass icon inside, placeholder "Cari produk..."
  Behavior: → On Enter: NAVIGATE /produk?q=[search-term] → Grid refreshes with search results
  States: Empty → Focus (border #B8924A, glow) → Filled

  [Element: Filter checkboxes "Kategori"]
  Options: Arabika, Robusta, Blend, Single Origin, Decaf
  Behavior: → STATE products grid refreshes with filter applied
  States: Unchecked (border #E0D0BB) → Checked (gold checkmark #B8924A)

  [Element: Filter checkboxes "Ukuran"]
  Options: 100g, 250g, 500g
  Behavior: → STATE products grid refreshes with filter applied

  [Element: Price range slider "Harga"]
  Style: Dual-range slider, min Rp 0 — max Rp 500.000, gold colored track #B8924A
  Behavior: → STATE products filtered by price range on release

  [Element: Filter radio buttons "Gilingan"]
  Options: Biji Utuh, Coarse, Medium, Fine
  Behavior: → STATE products grid refreshes with selected grind

  [Element: Tombol 'Terapkan Filter']
  Style: Primary gold button full-width at bottom of sidebar
  Behavior: → ACTION apply all selected filters → Grid refreshes → Active filter tags appear at top
  States: Default → Hover → Loading (spinner) → Disabled (if no filters changed)
  Annotation: 'ACTION → apply all filters → grid refreshes'

  [Element: Active filter tag pill × dismiss]
  Style: Gold tag pill with × icon, DM Sans 12px
  Behavior: → ACTION remove that specific filter → Grid refreshes
  Annotation: 'ACTION → remove filter → grid refreshes'

Right content area (remaining width):
  — Top row: "Menampilkan 48 produk" DM Sans 14px #6B4C35 left

  [Element: Sort dropdown]
  Style: DM Sans 14px, border #E0D0BB, radius 6px
  Options: "Terbaru" | "Terlaris" | "Harga: Rendah ke Tinggi" | "Harga: Tinggi ke Rendah" | "Rating Tertinggi"
  Behavior: → STATE products reordered by selected sort
  Annotation: 'STATE → reorder products'

  — Product grid: 3 columns, 24px gap, product cards same style as homepage (see PAGE 1 product card specs)
  Each card click → NAVIGATE /produk/[slug]

  [Element: Pagination]
  Style: Numbered pages, DM Sans 14px, active page bg #B8924A text #FAF7F2, arrow buttons
  Behavior: Each number → NAVIGATE /produk?page=[n] / Active page disabled / Arrow ← → previous/next page
  Annotation: 'NAVIGATE /produk?page=[n]'

  EMPTY STATE (when no products match filters):
  Illustration of empty coffee cup + text "Tidak ada produk yang cocok dengan filter Anda" Cormorant Garamond 500 24px + "Coba ubah atau reset filter untuk menemukan produk." DM Sans 14px #6B4C35
  [Element: Tombol 'Reset Filter']
  Style: Ghost button, text #6B4C35, underline on hover
  Behavior: → ACTION clear all filters → STATE grid shows all products
  Annotation: 'ACTION → reset all filters'

  LOADING STATE: Skeleton screens for product cards — warm beige shimmer (#E0D0BB to #F0E8D8), 3×2 grid of skeleton cards.

  ERROR STATE: Error icon + "Gagal memuat produk" message + "Coba Lagi" primary button → ACTION retry fetch


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PAGE 3 — DETAIL PRODUK (/produk/[slug])
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

INTENT: Present a single product in full detail with all variants, info, and reviews. Drive user to add the correct variant to cart. Every interaction on this page either configures the product or leads to purchase.

Breadcrumb top: "Beranda / Produk / Arabika Gayo Premium" in DM Sans 12px #6B4C35.
  "Beranda" → NAVIGATE / | "Produk" → NAVIGATE /produk | Current page name non-clickable

TWO-COLUMN LAYOUT (60/40 split):

LEFT — Image Gallery:
  — Main image: 560×560px, rounded 16px, warm product photo (coffee bag + beans)

  [Element: Thumbnail strip images]
  Style: 4 small thumbnails 80×80px each, rounded 8px
  Behavior: → STATE main image switches to clicked thumbnail, active thumbnail gets gold 2px border
  States: Default (border #E0D0BB) → Active (border 2px #B8924A) → Hover (border #9A7735)
  Annotation: 'STATE → switch main image'

  — Hover on main image: subtle zoom cursor + magnify hint

RIGHT — Product Info:
  — Category badge: "Arabika" sage green pill #7A9468 text #FAF7F2, DM Sans 500 12px, radius 4px
  — Product name: "Arabika Gayo Premium — Aceh" in Cormorant Garamond 600 36px
  — Rating row: 4.8 ★ (gold) | "128 ulasan" (link, underline → STATE scroll to #ulasan tab and activate it) | "Terjual 2.4rb" DM Sans 14px #6B4C35

  — Price: "Rp 89.000" DM Sans 600 28px #B8924A
    IF discounted: show crossed-out original "Rp 120.000" above in smaller gray DM Sans 400 16px line-through + "Diskon 25%" badge bg #C0392B text #FAF7F2
  — Divider line #E0D0BB

  [Element: Ukuran selector pills]
  Style: Label "UKURAN" DM Sans 500 12px uppercase #6B4C35 + 3 pill buttons
  Options: "100g" | "250g" | "500g"
  Behavior: → STATE variant selected → price updates to variant price, stock count updates
  States: Unselected (border 1.5px #E0D0BB, text #1C0A00) → Selected (bg #B8924A, text #FAF7F2) → Hover (border #B8924A) → Disabled/Out of Stock (bg #E0D0BB, text #A8886A, "Habis" tooltip)
  Annotation: 'STATE → select size variant → price + stock updates'

  [Element: Gilingan selector pills]
  Style: Same pill style as Ukuran
  Options: "Biji Utuh" | "Coarse" | "Medium" | "Fine" | "Extra Fine"
  Behavior: → STATE sub-variant selected
  States: Same as Ukuran
  Annotation: 'STATE → select grind type'

  [Element: Quantity control]
  Style: Bordered container, minus icon — number input — plus icon, DM Sans 500 16px
  Behavior:
    Minus (−) → STATE quantity decreases (min 1). IF qty = 1: minus button becomes DISABLED
    Plus (+) → STATE quantity increases (max = current variant stock). IF qty = max stock: plus button becomes DISABLED + tooltip "Stok maksimal"
  States: Default → Disabled (gray, cursor not-allowed)
  Annotation: 'STATE → adjust quantity (min 1, max stock)'

  — Stock info: "✓ Stok tersedia (143 unit)" in #7A9468 DM Sans 14px
    IF stock = 0 for selected variant: text changes to "✗ Stok habis untuk varian ini" in #C0392B

  [Element: Tombol 'Tambah ke Keranjang']
  Style: Primary gold button, full-width, 52px height, bg #B8924A, DM Sans 500 14px
  States:
    - Default: bg #B8924A, text 'Tambah ke Keranjang'
    - Hover: bg #9A7735, translateY -1px
    - Loading: spinner icon left + 'Memproses...' text, disabled
    - Disabled: bg #E0D0BB, text #A8886A (when no variant selected OR stock = 0)
  Behavior:
    - IF not logged in → NAVIGATE /login?redirect=/produk/[slug]
    - IF logged in + NO variant selected → button shakes + inline error "Pilih varian dahulu" appears below button in #C0392B DM Sans 12px
    - IF logged in + variant selected + stock > 0 → ACTION POST /cart/add → cart counter badge +1, success toast "Produk ditambahkan ke keranjang!" 3 detik
    - IF stock = 0 → button text changes to "Habis", DISABLED state
  Annotation: 'IF logged in + variant selected: POST /cart/add + toast / IF no variant: shake + error / IF not logged in: NAVIGATE /login'

  [Element: Heart/Wishlist button beside cart button]
  Style: Square button 52×52px, border 1.5px #E0D0BB, Phosphor Heart 20px center
  Behavior: → IF logged in: TOGGLE wishlist → icon fills gold (added) or outline (removed) + toast "Ditambahkan ke wishlist" or "Dihapus dari wishlist" / IF not logged in: NAVIGATE /login?redirect=/produk/[slug]
  States: Inactive (outline heart) → Active (filled gold heart) → Hover (scale 1.05)
  Annotation: 'IF logged in: TOGGLE wishlist / ELSE: NAVIGATE /login'

  — Delivery estimate: Phosphor Truck icon + "Estimasi pengiriman 2–4 hari kerja via Biteship" DM Sans 14px #6B4C35
  — Product highlights: 3 short icon+text rows: bean origin, roast date, altitude

── PRODUCT DESCRIPTION TABS ──
Below the two columns:

  [Element: Tab bar]
  Tabs: "Deskripsi" | "Informasi Produk" | "Ulasan (128)"
  Style: DM Sans 500 14px, active tab has #B8924A underline 2px, inactive #6B4C35
  Behavior: Each tab → STATE corresponding content panel shows, other panels hide
  Annotation: 'STATE → switch content panel'

  "Deskripsi" tab content: 2-column — left: long text description with tasting notes. Right: flavor wheel illustration.
  "Informasi Produk" tab content: spec table — Asal, Varietas, Proses, Ketinggian, Tanggal Panggang.
  "Ulasan (128)" tab content (id="ulasan"):
    — Review summary: big number 4.8, star distribution bar chart
    — List of review cards (user avatar, name, date, stars, comment text, optional photo)
    [Element: 'Lihat Semua Ulasan' link at bottom]
    Behavior: → STATE load more reviews (next 10) via pagination / IF all loaded: link hidden
    Annotation: 'STATE → load more reviews'

── RELATED PRODUCTS section at bottom ──
"Produk Terkait" H2 + horizontal 4-card scroll.
Each card → NAVIGATE /produk/[related-slug]


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PAGE 4 — KERANJANG BELANJA (/keranjang)
Requires login. IF not logged in → NAVIGATE /login?redirect=/keranjang
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

INTENT: Show users exactly what's in their cart, let them adjust quantities, apply vouchers, and move confidently to checkout. Minimize abandonment.

Page title: "Keranjang Belanja" H1 Cormorant Garamond left-aligned. Breadcrumb: "Beranda / Keranjang".
  "Beranda" → NAVIGATE /

TWO-COLUMN LAYOUT (65/35 split):

LEFT — Cart Items:
Header row: "Produk" | "Harga" | "Jumlah" | "Subtotal" — DM Sans 500 12px uppercase #6B4C35, borderBottom #E0D0BB.

Each cart item row (padding 24px 0, borderBottom #E0D0BB):
  — Product image 80×80px rounded 8px → NAVIGATE /produk/[slug] (clickable link to product)
  — Product info: name Cormorant Garamond 500 18px → NAVIGATE /produk/[slug] | variant info "250g · Medium Grind" DM Sans 12px #6B4C35
  — Unit price DM Sans 400 16px

  [Element: Quantity control (minus/number/plus)]
  Behavior:
    Minus → ACTION update qty in cart → qty -1 → subtotal recalculates. IF qty would become 0: show confirm dialog "Hapus produk ini dari keranjang?"
    Plus → ACTION update qty in cart → qty +1 → subtotal recalculates. IF qty = max stock: plus DISABLED + tooltip "Stok maksimal"
    Number input: direct edit → ACTION update qty → subtotal recalculates
  Annotation: 'ACTION → update cart qty → subtotal recalculates'

  — Subtotal DM Sans 500 16px #B8924A (auto-calculated: unit price × qty)

  [Element: Delete/trash icon]
  Style: Phosphor Trash 16px, #6B4C35
  Behavior: → ACTION show confirm dialog "Hapus produk ini dari keranjang?" → IF confirmed: remove item → cart refreshes → IF cart now empty: show empty state / IF cancelled: no action
  States: Default (#6B4C35) → Hover (#C0392B, scale 1.1)
  Annotation: 'ACTION → confirm → remove item from cart'

  [Element: Tombol 'Lanjut Belanja']
  Style: Ghost button with arrow-left icon, DM Sans 400 14px #6B4C35
  Behavior: → NAVIGATE /produk
  Annotation: 'NAVIGATE /produk'

RIGHT — Order Summary Card (sticky on scroll, bg #F0E8D8, rounded 16px, padding 28px):
Title: "Ringkasan Pesanan" DM Sans 500 18px.
Line items: Total Produk (Rp) | Ongkir ("Dihitung saat checkout" in DM Sans 12px #A8886A) | Diskon Voucher.

  [Element: Voucher input + 'Terapkan' button]
  Style: Text field "Kode voucher" + "Terapkan" secondary button (border #B8924A)
  Behavior: → ACTION validate voucher code →
    IF valid: discount amount applied, green badge "Voucher berhasil! Hemat Rp 50.000" appears, voucher tag with × shows
    IF invalid: error "Kode voucher tidak valid atau sudah kedaluwarsa" in #C0392B below input
    IF already applied: error "Hanya 1 voucher per pesanan"
  States: Empty → Focus (gold border) → Success (green) → Error (red border)
  Annotation: 'ACTION → validate voucher → apply discount or show error'

  [Element: Applied voucher tag × dismiss]
  Behavior: → ACTION remove voucher → total recalculates → voucher input reappears
  Annotation: 'ACTION → remove voucher → total recalculates'

Divider.
Total: "Total Pembayaran" DM Sans 500 18px + "Rp 267.000" Cormorant Garamond 600 32px #B8924A.

  [Element: Tombol 'Lanjut ke Checkout']
  Style: Primary gold button, full-width, 52px height, bg #B8924A, DM Sans 500 14px
  States:
    - Default: bg #B8924A, text 'Lanjut ke Checkout →'
    - Hover: bg #9A7735, translateY -1px
    - Loading: spinner + "Memproses..."
    - Disabled: bg #E0D0BB, text #A8886A (when cart is empty)
  Behavior:
    - IF cart is empty → button DISABLED (no action)
    - IF not logged in → NAVIGATE /login?redirect=/checkout
    - IF logged in + cart has items → NAVIGATE /checkout
  Annotation: 'NAVIGATE /checkout (requires login + non-empty cart)'

Below: padlock icon + "Transaksi aman dengan Midtrans" DM Sans 12px #6B4C35.
Payment icons strip: Visa, Mastercard, GoPay, OVO, bank icons (small, gray).

── EMPTY CART STATE ──
Show when cart has 0 items. Centered in left column:
  Coffee cup empty illustration + "Keranjang kamu kosong" H3 Cormorant Garamond 500 24px + "Yuk mulai belanja kopi terbaik!" DM Sans 14px #6B4C35
  [Element: Tombol 'Lihat Produk']
  Style: Primary gold button
  Behavior: → NAVIGATE /produk
  Annotation: 'NAVIGATE /produk'

LOADING STATE: Skeleton shimmer for cart items (warm beige #E0D0BB to #F0E8D8).


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PAGE 5 — CHECKOUT (/checkout)
Requires login. IF not logged in → NAVIGATE /login?redirect=/checkout
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

INTENT: Collect shipping address, select courier, choose payment method, and finalize order. Minimize friction — pre-fill saved data where possible. Build trust with security indicators.

Progress bar at top (below navbar): 3 steps — "1 Keranjang" → "2 Checkout" → "3 Konfirmasi" — step 2 active in gold, step 1 completed with checkmark.
  Step 1 "Keranjang" → NAVIGATE /keranjang (clickable, since completed)
  Step 2 "Checkout" → current page, non-clickable
  Step 3 "Konfirmasi" → not yet accessible, grayed out

TWO-COLUMN LAYOUT (60/40 split):

LEFT — Checkout Form:

SECTION A — Alamat Pengiriman:
Heading "Alamat Pengiriman" H3. 

  [Element: Address radio cards]
  IF user has saved addresses: show 2–3 address radio cards (rounded 8px, border 1.5px #E0D0BB). Each card: label ("Rumah"), recipient name, full address.
  Behavior: → STATE selects that address for delivery → border changes to 2px #B8924A (selected)
  States: Unselected (border #E0D0BB) → Selected (border 2px #B8924A, bg rgba(184,146,74,0.05))
  Annotation: 'STATE → select shipping address'

  [Element: '+ Tambah Alamat Baru' ghost button]
  Style: Dashed border card or ghost button with + icon
  Behavior: → STATE address form expands inline below existing addresses with smooth slide-down animation
  Annotation: 'STATE → expand new address form'

  New address form fields (16px gap):
  — Nama Penerima (text, required*)
  — No. Telepon (tel, required*, +62 prefix)

  [Element: Provinsi dropdown]
  Behavior: → On select: ACTION FETCH cities from Biteship API → populates Kota/Kabupaten dropdown
  States: Empty → Loading (spinner inside) → Populated → Error ("Gagal memuat data")
  Annotation: 'ACTION → FETCH Biteship cities API'

  [Element: Kota/Kabupaten dropdown]
  Behavior: → On select: ACTION FETCH districts from Biteship API → populates Kecamatan dropdown
  Annotation: 'ACTION → FETCH Biteship districts API'

  — Kecamatan (dropdown, populated via Biteship)
  — Kode Pos (auto-filled from Biteship, editable)
  — Alamat Lengkap (textarea, required*)
  — Label Alamat: pill select "Rumah" | "Kantor" | "Lainnya"

SECTION B — Metode Pengiriman:
Heading "Metode Pengiriman" H3.

  IF address not yet filled: show helper text "Dihitung otomatis berdasarkan alamat" DM Sans 14px #A8886A
  IF address filled: ACTION FETCH courier options from Biteship API → show radio cards

  [Element: Courier option radio cards]
  Style: Each card border 1.5px #E0D0BB, rounded 8px, padding 16px
  Content: courier logo (JNE/J&T/SiCepat/Anteraja) + service name (REG/YES/OKE) + estimated days + price in #B8924A
  Behavior: → STATE selects courier → shipping cost appears in order summary → total recalculates
  States: Unselected → Selected (border 2px #B8924A) → Loading (skeleton shimmer while fetching)
  Annotation: 'STATE → select courier → total updates'

SECTION C — Metode Pembayaran (via Midtrans):
Heading "Metode Pembayaran" H3. Subtext: "Powered by Midtrans — Transaksi Aman & Terenkripsi." DM Sans 12px #6B4C35

  [Element: Payment method radio cards]
  Style: 4 cards in 2×2 grid, border 1.5px #E0D0BB, rounded 8px
  Options:
    1. "Transfer Bank" — bank icons (BCA, BNI, BRI, Mandiri) → STATE select transfer method
    2. "Virtual Account" — same banks, VA number generated → STATE select VA method
    3. "GoPay / OVO / DANA" — e-wallet logos → STATE select e-wallet method
    4. "Kartu Kredit/Debit" — Visa + Mastercard logos → STATE select card method
  Each card: icon left, name DM Sans 500 14px, description DM Sans 12px #6B4C35
  States: Unselected → Selected (border 2px #B8924A, checkmark icon)
  Annotation: 'STATE → select payment method'

  Note below: "Kamu akan diarahkan ke halaman pembayaran Midtrans" DM Sans 12px #6B4C35

SECTION D — Catatan Pesanan:
  [Element: Catatan textarea]
  Style: Optional textarea "Catatan untuk penjual (opsional)" — small, 80px height, border #E0D0BB
  States: Empty → Focus (gold border) → Filled

RIGHT — Order Summary (sticky, same style as cart page summary card):
Title "Pesanan Kamu".
List each item: small thumbnail 48×48px + name + variant + qty × price.
Divider.
Cost breakdown:
  Subtotal produk | Ongkos kirim (from Biteship, or "Pilih pengiriman" if not yet chosen) | Diskon voucher (if applied) | Total.
  Voucher already applied shows green discount.
"Rp 298.000" total in large Cormorant Garamond 600 32px #B8924A.

  [Element: Tombol 'Bayar Sekarang']
  Style: Primary gold button, full-width, 52px height
  States:
    - Default: bg #B8924A, text "Bayar Sekarang"
    - Hover: bg #9A7735, translateY -1px
    - Loading: spinner + "Memproses pesanan..."
    - Disabled: bg #E0D0BB (when required fields incomplete — address, courier, or payment not selected)
  Behavior:
    - IF any required field invalid → show field-level errors, scroll to first error
    - IF all fields valid + payment = Midtrans (VA/e-wallet/card) → ACTION create order → EXTERNAL Midtrans payment popup/redirect
    - IF all fields valid + payment = Transfer Bank → ACTION create order → NAVIGATE /checkout/pembayaran
    - IF order creation fails → error toast "Gagal membuat pesanan. Silakan coba lagi."
  Annotation: 'IF valid: ACTION create order → Midtrans or /checkout/pembayaran'

Security badges row: Midtrans logo + Biteship logo + padlock icon + "SSL Secured" text in DM Sans 11px #6B4C35.

── FORM VALIDATION ERRORS ──
All required fields marked with * in label. On submit with errors:
  - Address empty: "Pilih atau tambah alamat pengiriman" 
  - Courier not selected: "Pilih metode pengiriman"
  - Payment not selected: "Pilih metode pembayaran"
  All errors: DM Sans 12px #C0392B below the respective section heading.


════════════════════════════════════════
ANNOTATION & COMPONENT INSTRUCTIONS
════════════════════════════════════════

For every interactive element in every page, add a small Figma annotation label (sticky note style, yellow background) showing: element name → action → destination. Place annotations outside the frame boundary so they don't clutter the design but remain attached to their element.

Every button, form field, and card must include a Figma component description note specifying: purpose, trigger event, expected outcome, and all visible states (default/hover/active/disabled/loading/error/success).